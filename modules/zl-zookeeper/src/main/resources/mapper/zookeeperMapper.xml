<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bj.zl.learn.zookeeper.main.dao.ZookeeperDao">
    <select id="check" resultType="java.lang.Integer">
        select i from test WHERE  id = 1;
    </select>
    
    <update id="des" parameterType="java.lang.Integer">
        UPDATE test SET i = #{obj} WHERE id = 1
    </update>

</mapper>